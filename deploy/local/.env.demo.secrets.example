# Copy to deploy/local/.env.demo.secrets (gitignored) and fill locally.
# Never commit real keys.

DEEPSEEK_API_KEY=
# Optional overrides:
# OPENCODE_SERVER_PASSWORD=probe-secret
# BROKER_URL=http://127.0.0.1:8080
# JWT_SECRET=local-dev-secret
